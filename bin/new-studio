#!/usr/bin/env bash
set -eu

if [ "${1:-}" = "--help" ] || [ "${1:-}" = "-h" ]; then
    cat <<'EOF'
Usage: new-studio [options]

This command is specified in docs/SCRIPT_REFERENCE.md but is not implemented
until Batch 3.
EOF
    exit 0
fi

echo "new-studio is not implemented until Batch 3." >&2
exit 2

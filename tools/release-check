#!/usr/bin/env bash
# Batch 3 release-check placeholder.
# Full archive construction and install/upgrade/uninstall verification belong to Batch 4.
set -eu

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

make strict-test

echo "[OK] Batch 3 release check passed"
